# The corpus files are served byte-for-byte to machine readers. Git's autocrlf
# would rewrite them to CRLF on a Windows checkout, which changes every line of
# an NDJSON stream and breaks parsers that split strictly on \n.
*.jsonl text eol=lf
*.json  text eol=lf
llms.txt text eol=lf
