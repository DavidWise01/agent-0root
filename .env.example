# agent-0root — environment (copy to .env for local; set real values in Railway dashboard)
PORT=8000
# Railway injects RAILWAY_GIT_COMMIT_SHA automatically; GIT_SHA is a manual fallback.
GIT_SHA=
# add your own secrets here (DB urls, api keys) — never commit the real .env
